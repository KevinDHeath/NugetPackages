# Uploads the release packages to NuGet
name: 📦 Upload to NuGet

on:
  workflow_dispatch:
    inputs:
      run-name:
        description: A name for the run
        type: string
        required: true
      project-folder:
        description: Folder containing the solution
        type: string
        default: .

run-name: ${{ inputs.run-name }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      BUILD_CONFIGURATION: Release
      PACKAGE_FOLDER: ${{ inputs.project-folder }}/local

    defaults:
      run:
        working-directory: ${{ inputs.project-folder }}

    steps:
      - uses: actions/checkout@v4

      - name: Create packages folder
        run: mkdir ${{ env.PACKAGE_FOLDER }}

      - name: .NET restore
        run: dotnet restore

      - name: .NET build
        run: dotnet build --no-restore -c ${{ env.BUILD_CONFIGURATION }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Local-Packages
          path: ${{ env.PACKAGE_FOLDER }}
          retention-days: 5

      - name: NuGet push
        env:
          NUGET_URL: "https://apiint.nugettest.org/v3/index.json"
        shell: pwsh
        run: |
          foreach($file in (Get-ChildItem "${{env.PACKAGE_FOLDER}}" -Recurse -Include *.nupkg))
          { dotnet nuget push $file -k "${{secrets.NUGET_TEST_TOKEN}}" -s "${{env.NUGET_URL}}" --skip-duplicate }
